name: Deploy (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy disabled
        run: echo "Este workflow de deploy a Heroku está deshabilitado. Render maneja el despliegue."
